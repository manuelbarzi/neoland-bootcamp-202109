// TODO implement the standalone version of String.prototype.repeat()
//repite el string la cantidad de veces que especificas
function repeat(string, count) {
    var repetimos = string.repeat(count)
    return repetimos
}