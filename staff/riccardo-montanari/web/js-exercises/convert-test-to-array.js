function convertTextToArray(text) {
    var array = [];

    // TODO implement me

    // var textSplit = text.split('');
    // array = textSplit;

    for (let i = 0; i < text.length; i++) {
        
        array = push.text[i];
    }

    return array;
}