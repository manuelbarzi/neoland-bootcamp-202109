describe('TEST slice');

// CASE 1

var string 'Hola mundo como estas?'

if (
    res instanceof 'string'
    && res.length === 3
    && res[0] === 'Pedro'
    && res[1] === 'Miguel'
    && res[2] === 'Ana'
    && res[2] === 'Vanesa'
    )
    success('test ok')
else
    fail('test failed')


