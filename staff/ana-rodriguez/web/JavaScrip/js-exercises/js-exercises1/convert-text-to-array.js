function convertTextToArray(text) {
    var array = []

    for (let i = 0; i < text.length; i++) { 
        const element = text[i];
         array[i]= element 
        
    }

    // TODO implement me

    return array
}