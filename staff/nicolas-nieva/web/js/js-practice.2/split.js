function split (text, pattern) {
    var part = []
    var parts = ''

    for (let i = 0; < text.length; i++) {
              caracter = text[i];

              if (caracter === separator)
   
    }
}  