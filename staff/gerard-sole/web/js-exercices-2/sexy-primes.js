// Los primos sexy son pares de dos primos que están
// separados por 6
function sexyPrimes(a, b) {
    let itsPrime
    if (a - b === 6)
    for (let i = 2; i < number; i++) {
        if (number % i == 0) {
            isPrime = false;
            break;
    


    return itsPrime 
}